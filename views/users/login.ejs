<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Sign In | The Wall</title>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
        <script src="/assets/js/login.js"></script>
    </head>
    <body>
        <div id="wrapper">
            <h1>Welcome to "The Wall"</h1>
            <p>Sign in to you account</p>

            <form action="/login" id="login_form" method="POST">
<% if(locals.message){ %>
                <p class="message"><%= locals.message %></p>
<% } %>
                <div>
                    <label for="email">
                        Email Address:
                        <input type="email" id="email" name="email_address" placeholder="Enter your email" value="clifford@example.com">
                    </label>
                    <!-- ERROR MESSAGE WILL BE HERE LATER -->
<% if(locals.errors && locals.errors.email){ %>
                    <p class="email"><%= locals.errors.email %></p>
<% } %>
                </div>
                
                <div>
                    <label for="password">
                        Password:
                        <input type="password" id="password" name="password" placeholder="Enter your password" value="@dmin_123">
                    </label>
                    <!-- ERROR MESSAGE WILL BE HERE LATER -->
<% if(locals.errors && locals.errors.password){ %>
                    <p class="password"><%= locals.errors.password %></p>
<% } %>
                </div>

                <input type="submit" value="Sign in">
                <a href="/register">Don't have an account?</a>
            </form>
        </div>
    </body>
</html>